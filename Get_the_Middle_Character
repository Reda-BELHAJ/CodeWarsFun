def get_middle(s):
    Middle = int(len(s) / 2)
    
    if len(s) >= 3:
        if len(s) % 2 == 0:
            return ''+ s[Middle - 1] + s[Middle]
        else:
            return s[Middle]
    else:
        return s
